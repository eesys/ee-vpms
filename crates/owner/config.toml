# Owner Microservice Configuration
# This service handles owner-related business logic

[direct]

[direct.services]

# Owner service's own listen address (used in get_service_listen_address)
[direct.services.owner]
addr = "http://[::1]:50051"
resolver_type = "direct"

# Database configuration
[database]
url = "postgres://postgres:postgres@localhost:5432/ee_vpms"
